apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: default
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
    nodePort: 30501
